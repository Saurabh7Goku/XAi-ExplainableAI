import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MangoAI - Leaf Disease Detector | XAI-Powered",
  description: "Advanced AI-powered mango leaf disease detection using Vision Transformers and Explainable AI. Upload a leaf photo for instant diagnosis with visual explanations.",
  keywords: ["mango leaf disease", "AI agriculture", "plant disease detection", "Vision Transformer", "XAI", "Explainable AI"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
